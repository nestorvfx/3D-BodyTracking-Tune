# BlazePose tune — Vast.ai TRAINING-TIME dependency pin list.
# All entries are commercial-clean (Apache-2.0 / BSD / MIT / PSF).
# CUDA wheels: torch is installed separately by vast_setup.sh (GPU-arch-aware).
# EXPORT-TIME deps (`litert-torch` + `tf-nightly`) are installed by
# vast_train.sh stage 6 only — keeps the training env free of tensorflow,
# which conflicts with PyTorch nightly cu128.

# Pose / vision
mediapipe==0.10.33                # Apache-2.0 — runs Heavy/Hand teachers
opencv-python==4.13.0.92          # Apache-2.0 — image I/O, augmentation
albumentations==2.0.8             # MIT — augmentation pipeline
Pillow>=10.0,<13                  # MIT-CMU
scikit-image>=0.24,<1             # BSD

# TFLite schema parser (used by our custom port + custom export, no third-party libs)
tflite==2.18.0                    # Apache-2.0
flatbuffers>=24.3                 # Apache-2.0 — used by custom export-rewriter fallback

# Numerical
numpy>=2.0,<3                     # BSD-3 + others
scipy>=1.13,<2                    # BSD-3

# Training infra
tensorboard>=2.18,<3
tqdm>=4.66,<5
pyyaml>=6.0,<7

# Ego-Exo4D download CLI (MIT) + AWS S3 access
ego4d>=1.7.3
awscli>=1.33,<2

# Hugging Face transfer (synth dataset is on HF)
huggingface_hub>=0.27,<1
hf_transfer>=0.1.6                # Apache-2.0 — fast HF downloads
